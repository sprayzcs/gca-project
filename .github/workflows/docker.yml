on:
  push:

jobs:
  build-push-frontend:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      -
        name: Set up environment
        run: echo "BRANCH=${GITHUB_REF##*/}" >> $GITHUB_ENV
      -
        name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          file: Frontend/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_IMAGE_FRONTEND }}:latest,${{ secrets.DOCKER_IMAGE_FRONTEND }}:${{ env.BRANCH }}
          build-args: |
            BRANCH=${{ env.BRANCH }}
  
  build-push-services:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      -
        name: Set up environment
        run: echo "BRANCH=${GITHUB_REF##*/}" >> $GITHUB_ENV
      -
        name: Build and push catalog
        uses: docker/build-push-action@v3
        with:
          context: .
          file: Backend/Services/CatalogService/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_IMAGE_CATALOG }}:latest,${{ secrets.DOCKER_IMAGE_CATALOG }}:${{ env.BRANCH }}
          build-args: |
            BRANCH=${{ env.BRANCH }}
      -
        name: Build and push cart
        uses: docker/build-push-action@v3
        with:
          context: .
          file: Backend/Services/CartService/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_IMAGE_CART }}:latest,${{ secrets.DOCKER_IMAGE_CART }}:${{ env.BRANCH }}
          build-args: |
            BRANCH=${{ env.BRANCH }}
      -
        name: Build and push checkout
        uses: docker/build-push-action@v3
        with:
          context: .
          file: Backend/Services/CheckoutService/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_IMAGE_CHECKOUT }}:latest,${{ secrets.DOCKER_IMAGE_CHECKOUT }}:${{ env.BRANCH }}
          build-args: |
            BRANCH=${{ env.BRANCH }}
      -
        name: Build and push shipping
        uses: docker/build-push-action@v3
        with:
          context: .
          file: Backend/Services/ShippingService/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_IMAGE_SHIPPING }}:latest,${{ secrets.DOCKER_IMAGE_SHIPPING }}:${{ env.BRANCH }}
          build-args: |
            BRANCH=${{ env.BRANCH }}
